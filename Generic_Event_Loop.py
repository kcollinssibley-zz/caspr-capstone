# Takes call back function & a trigger
# Listens to trigger (socket), then runs call-back function




# Down the road: if we make custom protocal - could handle that as well
# construct/deconstruct custom packets